<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.formpendaftaranmahasiswa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FormPendaftaranMahasiswa">

        <!-- Login Activity sebagai launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home Activity -->
        <activity
            android:name=".Activity_Home"
            android:exported="false" />

        <!-- List Activities -->
        <activity
            android:name=".ActivityList_1"
            android:exported="false" />

        <activity
            android:name=".ActivityList_2"
            android:exported="false" />

        <activity
            android:name=".ActivityList_3"
            android:exported="false" />

    </application>

</manifest>